"""HTTP adapters. Import the one matching your framework; both expose the same routes.

Kept as thin translation layers — all behaviour lives in the framework-free core, so adding a
third framework is a small file, not a port.
"""
